# build output
/agentdeck
/agentdeck-*
*.test

# python (the e2e suite + the agent-side scripts) caches
.venv/
__pycache__/
*.pyc
.pytest_cache/

# runtime state (never commit)
agentdeck.db*
agentdeck-diffs/
smoke-repo/
.agentdeck-worktrees/
.playwright-mcp/

# local backups / scratch
*.pre-v02-schema
*.bak-*

# secrets — config is via environment only
*.env
!deploy/*.example.env

# screenshots written by the `verify` suite
.verify-artifacts/

# Local verification output and dependency caches
node_modules/
